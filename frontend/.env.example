# Example frontend environment variables (DO NOT store real secrets here)
# Copy to .env and fill with real values when running locally or in CI.

# Backend base URL used by the frontend
VITE_BACKEND_URL=https://aichatbot-yd2o.onrender.com

# Origins allowed to embed the widget (for local dev examples)
WIDGET_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# When serving frontend from backend for local testing
SERVE_FRONTEND=true
